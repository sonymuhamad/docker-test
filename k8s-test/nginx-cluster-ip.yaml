apiVersion: v1
kind: Service
metadata:
  name: nginx-cluster-ip
  labels:
    app: nginx-cluster
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
